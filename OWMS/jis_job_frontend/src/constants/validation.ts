/**
 * 전역 유효성 검사 상수
 *
 * 이 파일의 값을 수정하면 시스템 전체에 적용됩니다.
 */

// ============================================================
// 일일업무 보고 관련 제한
// ============================================================

/**
 * 업무 제목/내용 1줄당 최대 글자수
 * - 일일업무 보고의 업무명, 업무내용
 * - 업무 목록 관리의 업무명, 거래처명
 */
export const MAX_CHARS_PER_LINE = 20;

/**
 * 주간 정보 최대 줄 수
 */
export const MAX_WEEKLY_NOTE_LINES = 4;

/**
 * 주간 정보 1줄당 최대 글자수
 */
export const MAX_WEEKLY_NOTE_CHARS_PER_LINE = 40;

// ============================================================
// 업무 목록 관리 관련 제한
// ============================================================

/**
 * 업무명(프로젝트명) 최대 글자수
 * 주간업무 작성 시 "거래처 : 업무명" 형식으로 표시되므로
 * 전체 길이가 MAX_CHARS_PER_LINE을 초과하지 않도록 제한
 */
export const MAX_PROJECT_NAME_LENGTH = MAX_CHARS_PER_LINE;

/**
 * 거래처명 최대 글자수
 */
export const MAX_CLIENT_NAME_LENGTH = 10;

/**
 * 업무명 + 거래처명 결합 시 최대 글자수
 * "거래처 : 업무명" 형식 (구분자 포함)
 */
export const MAX_COMBINED_JOB_TITLE_LENGTH = MAX_CHARS_PER_LINE;
